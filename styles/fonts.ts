const families = {
  default: 'Arial, Helvetica, sans-serif',
};

const sizes = {
  xs: '1rem',
  s: '1.25rem',
  m: '1.5rem',
  l: '1.75rem',
  xl: '2rem',
  xxl: '2.5rem',
};

const fonts = {
  families,
  sizes,
};

export default fonts;
